import 'package:flutter/material.dart';

class CharacterCampaignModalScreen extends StatelessWidget {
  const CharacterCampaignModalScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
