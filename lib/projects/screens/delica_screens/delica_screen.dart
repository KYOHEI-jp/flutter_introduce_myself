import 'package:flutter/material.dart';

class DelicaWorksScreen extends StatelessWidget {
  const DelicaWorksScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
